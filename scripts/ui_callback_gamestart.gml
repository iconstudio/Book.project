/// ui_callback_gamestart();

//show_debug_message("Script ui_callback_gamestart");

with (zui_get_parent()) { /// oUIMainFrame (0)
 selected = 0;

 with (zui_get_parent()) /// oMainMenu
  menu_main_update(1);

}

