///ui_callback_newgame(index);

PLAYER_SAVEDLOADED_INDEX = argument0;

game_room_set_size(768 * 3, 768 * 3);

with (zui_get_parent()) {

 with (zui_get_parent()) {
  endaction = 1;
  close = true;
 }
}

